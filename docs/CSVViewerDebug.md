# CSV Viewer 调试指南

## 已添加的调试输出

### 前端（浏览器控制台）

查看 DevTools Console，会看到以下日志：

#### 文件加载流程
```
🚀 开始加载文件: [文件路径]
📡 调用后端 csv_load_file...
✅ 后端返回: { path, totalRows, delim }
📄 小文件模式: [行数] 行, 直接加载
  或
📄 大文件模式: [行数] 行, 需要分页

📦 处理小文件, 总行数: [数字]
📡 获取分页信息...
✅ 分页信息: [对象]
📡 加载第一页数据...
✅ 数据解析完成: { headers, rows, skipped }
✅ 小文件处理完成
✅ 文件加载完成
```

#### 错误情况
```
❌ 加载失败: [错误信息]
错误详情: { message, stack }
```

### 后端（终端输出）

查看运行 `pnpm tauri dev` 的终端，会看到：

#### 文件加载
```
🚀 [Backend] csv_load_file 开始, 文件: [路径]
📁 [Backend] 读取文件中...
✅ [Backend] 文件读取成功, 大小: [字节数] bytes
🔍 [Backend] 检测分隔符...
✅ [Backend] 分隔符: ','
📊 [Backend] 统计行数...
✅ [Backend] 总行数: [数字]
💾 [Backend] 缓存文件内容...
✅ [Backend] csv_load_file 完成
```

#### 页面加载
```
📄 [Backend] csv_load_page 开始, 页码: 0, 行范围: 0-200000
📦 [Backend] 从缓存获取文件内容...
📊 [Backend] 文件大小: [字节] bytes, 分隔符: ','
🔄 [Backend] 开始解析页面...
✅ [Backend] 解析完成: [列数] 列, [行数] 行
💾 [Backend] 缓存页面数据...
✅ [Backend] csv_load_page 完成
```

## 测试步骤

### 1. 启动应用并查看日志
```bash
pnpm tauri dev
```

### 2. 打开浏览器 DevTools
- Windows: `F12` 或 `Ctrl+Shift+I`
- Mac: `Cmd+Option+I`

### 3. 加载你的 CSV 文件
文件名: `Cycl_T25_SOC10-90_Dch0.5C_Ch0.5C_Vito_Cell78_AllData.csv`

### 4. 查看日志输出

#### 在浏览器控制台看到什么？
记录输出并发送给我，特别是：
- ✅ 成功的步骤
- ❌ 失败的步骤
- 错误详情

#### 在终端看到什么？
记录后端输出，特别注意：
- 文件是否成功读取？
- 文件大小是多少？
- 行数统计是多少？
- 在哪一步失败？

## 常见问题排查

### 问题 1: 文件读取失败
```
❌ [Backend] 文件读取失败: No such file or directory
```
**原因**: 文件路径不正确
**解决**: 确保文件路径存在且有读取权限

### 问题 2: 解析失败
```
❌ [Backend] 解析失败: [错误信息]
```
**可能原因**:
- CSV 格式不标准
- 编码问题
- 内存不足

### 问题 3: 前端调用失败
```
❌ 加载失败: [错误信息]
```
查看错误详情的 `message` 和 `stack`

## 拖拽功能说明

当前版本：
- ✅ 支持拖拽检测
- ⚠️ 但 Tauri 环境下无法直接使用拖拽的 File 对象
- 💡 建议使用点击按钮选择文件

拖拽时会显示提示：
```
拖拽功能暂不支持，请点击按钮选择文件
```

## 需要提供的信息

如果遇到问题，请提供：

1. **浏览器控制台的完整日志**
2. **终端的完整后端日志**
3. **文件信息**:
   - 文件大小
   - 行数（如果知道）
   - 前几行内容示例
4. **错误发生的具体步骤**

## 临时解决方案

如果新版本有问题，可以切换回旧版本：

```typescript
// 在 src/Tools/Datascope/index.ts
export const datascopeToolConfig: ToolConfig = {
  component: () => import("./Datascope"), // 使用前端版本
  // component: () => import("./DatascopeBackend"), // 后端版本
}
```

然后重启应用：
```bash
pnpm tauri dev
```
