// src/Settings/index.ts
export { default as SettingsPage } from './SettingsPage';

// 设置路由注册函数
export function registerSettingsRoute() {
  // 设置路由已经在 Router 的 defaultRoutes 中定义
  // 不需要额外注册,这里保持接口一致性
}
